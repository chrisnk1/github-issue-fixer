{
    "name": "@fix-together/mcp-client",
    "version": "0.1.0",
    "type": "module",
    "description": "MCP server integration layer",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "test": "vitest",
        "typecheck": "tsc --noEmit",
        "clean": "rm -rf dist *.tsbuildinfo"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^0.5.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "@types/node": "^20.11.0",
        "typescript": "^5.3.3",
        "vitest": "^1.2.0"
    }
}